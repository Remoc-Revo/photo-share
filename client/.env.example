# No client-side environment variables are needed for this project by default.
# If you need to expose variables to your Vite app, prefix them with VITE_
# Example: VITE_API_URL=http://localhost:3001/api
